<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="textStyle">
        <item name="android:layout_marginLeft">@dimen/magnitude_margin</item>
        <item name="android:layout_marginStart" tools:targetApi="jelly_bean_mr1">@dimen/magnitude_margin</item>
        <item name="android:layout_marginTop">@dimen/magnitude_margin</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="subTextStyle" parent="textStyle">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.SearchResult.Subtitle</item>
    </style>

    <style name="HeadTextStyle" parent="textStyle">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.SearchResult.Title</item>
    </style>



</resources>
